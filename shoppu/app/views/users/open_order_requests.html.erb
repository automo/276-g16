<div class="input_section">
  <% @all_order_requests.each_with_index do |order_request, i| %>
    <div class="index_row clearfix">
      <h2 class="order_request_title"><%= i+1 %>.&nbsp;<%= order_request.title %> - <%= order_request.owner.username %></h2>
      <p class="order_request_sub_title"><%= order_request.description %></p>

      <% order_request.order_items.each do |order_item| %>
        <div class="order_item">
          <p><%= order_item.content %></p>
        </div>
      <% end %>

    </div>
  <% end %>
<div>
